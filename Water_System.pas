Program Buddha_8bit;
Const 
    Median = 0.81;
    Power = 0.5;
Var 
    w : Real;
Begin;
    ReadLn (w)
    IF (w < Median) Then
		WriteLn (Power);
end.

{ by Tovarisch Trunaev, 2026, Russia, Uray }
